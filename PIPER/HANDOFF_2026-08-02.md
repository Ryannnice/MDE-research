# PIPER 今日进度与明日恢复手册（2026-08-02）

> 目标：明天不用回看聊天记录，按本文即可恢复到今天结束时的连接状态。
>
> 本文不保存任何密码、HTTP 令牌或 SSH 私钥内容。聊天中曾出现过服务器密码，建议后续更换；目前服务器到 Windows 已改为密钥登录，不再需要该密码。

## 一句话结论

PIPER、原厂夹爪、Windows 远程登录、开机自启的只读桥和 D455 都已打通；机械臂与夹爪反馈健康。D455 已固定在末端附近并通过 Windows USB 3.x 完成 RGB、对齐深度和 IMU 联调。下一步是固定线缆、做相机到末端的眼在手上标定，并建立相机/机械臂/夹爪同步采集。

## 明天 60 秒恢复

### 1. 现场检查

- PIPER 电源已打开。
- USB-CAN 仍插在 Windows 电脑上。
- 夹爪线保持当前接法，不要改动两个圆形接口。
- D455 保持用 USB 3.x 线直接连接 Windows；检查线缆有应力释放，不会进入夹爪或关节运动范围。
- 完成外参标定后不要再改变相机支架位置或角度，否则必须重做标定。
- 机械臂当前各关节仍为使能状态。断电前必须托住机械臂，避免突然下落。

### 2. 在学校服务器执行

```bash
cd /renyuanliu/MDE-research

# 验证服务器可以免密进入 Windows
ssh -o BatchMode=yes piper-windows hostname

# 验证 Windows 上的开机自启观察桥
ssh piper-windows 'C:\Desktop\PIPER\remote_control\.venv\Scripts\python.exe C:\Desktop\PIPER\remote_control\piper_client.py --url http://127.0.0.1:57845 health'

# 读取机械臂和夹爪完整状态；这是只读操作
ssh piper-windows 'C:\Desktop\PIPER\remote_control\.venv\Scripts\python.exe C:\Desktop\PIPER\remote_control\piper_client.py --url http://127.0.0.1:57845 state'

# 检查 D455；插好后应打印型号、序列号、固件和 USB 类型
ssh piper-windows 'C:\Desktop\PIPER\realsense\rs-enumerate-devices.exe'
```

预期的健康结果：

```json
{
  "ok": true,
  "bridge_version": "0.5.1",
  "mode": "observe"
}
```

如果前三条成功，就不需要再打开旧的 8766 隧道窗口，也不需要重新安装任何文件夹。

## 当前连接结构

```text
PIPER + AGILE.X 夹爪 ──24 V/CAN──> USB-CAN 适配器 ──USB──┐
        │                                                │
        └──机械支架──> D455 ───────────────USB 3.x────────┤
                                                         ▼
                                                     Windows 11
                                                         │
                           ┌─ PIPER 观察桥：127.0.0.1:57845
                           └─ OpenSSH：127.0.0.1:22
                                      │
                                      │ 开机自启反向 SSH 隧道
                                      ▼
学校服务器：127.0.0.1:22022
        │
        └─ SSH 别名：piper-windows
```

从个人 Windows/VS Code 进入学校服务器仍使用原来的主机别名：

```text
Host：3090x6
HostName：10.27.130.23
User：root
Port：32736
```

密码不记录在本文。进入学校服务器后，再通过 `piper-windows` 进入现场 Windows 主机 `ENVY_Katana`。

重要区别：

- 夹爪通过机械臂末端的 24 V/CAN 接口通信。
- 相机只是机械固定在机械臂或场景支架上，电气上仍通过 USB 连接 Windows。
- PIPER 本身不是 USB 集线器，末端两个两针圆口不能连接 D455。

## 今天完成的工作

### 1. 建立 Windows 版 PIPER 控制桥

服务器和 Windows 当前使用同一套 `remote_control` v0.5.1：

- Windows 路径：`C:\Desktop\PIPER\remote_control`
- 服务器路径：`/renyuanliu/MDE-research/PIPER/remote_control`
- Python：3.12.5
- `python-can`：4.6.1
- `pyAgxArm`：1.0.0
- `python-can-agx-cando`：0.0.1
- CAN 接口：`agx_cando`，通道 `0`，1 Mbps

为避免 Windows 无法访问 GitHub，依赖已放入 `vendor/` 并离线安装，固定源码版本为：

- `agilexrobotics/pyAgxArm@799b8412fbe8b9156bc9892d3dbeb2df7e98be71`
- `agilexrobotics/python-can-agx-cando@b222c4027ad4f6599f7634c72c67184619177972`

桥接程序具备：

- `observe` 只读模式和 `control` 控制模式；
- 固件档位识别；
- 关节反馈、健康状态和错误状态读取；
- 现场许可、预览/执行分离和电子停止；
- 大动作自动拆分为小航点；
- 原厂夹爪反馈、回零、宽度和力参数控制；
- HTTP 令牌认证；
- 对 S-V1.8-9 夹爪回零位不可靠的兼容处理。

服务器端 37 项自动测试已于今天结束前重新运行，全部通过。

### 2. 打通 PIPER CAN 通信

已识别 Windows 设备：

```text
candleLight USB to CAN adapter        OK
candleLight firmware upgrade interface OK
```

机械臂已确认：

- 型号：普通 PIPER
- 硬件：`H-V1.2-1`
- 固件：`S-V1.8-9`
- 固件参数：`v189`
- 节点号：`15`
- 状态：`NORMAL`
- 错误码：`0`
- 六个关节反馈稳定，典型频率 190–240 Hz

### 3. 完成分级实机运动验证

在现场有人、急停可用并逐次确认的条件下，今天完成了：

- `prepare`：切换到 `CAN_CTRL / MOVE_J`，不发送运动目标；
- J2/J3 回零方向动作；
- J6 正向 1°；
- J6 的 `-1°、+2°、-2°` 序列；
- 较大幅度的“竖直指向上方—回到原位”循环测试。

现场没有报告异常。今天没有建立持久化运动命令日志，因此后续复现实验应新增带时间戳的命令、目标、反馈和结果日志，不能只依靠聊天记录。

### 4. 接通并验证原厂夹爪

夹爪已正确接到 PIPER 末端 24 V/CAN 线束，并完成：

- 反馈识别；
- 故障位检查；
- 手动完全闭合；
- 写入零点；
- 小行程张开测试；
- 动作结束后关闭夹爪驱动。

当前反馈约为：

```text
开口：9.8 mm
驱动：disabled
通信：healthy
故障：全部 false
```

固件 `S-V1.8-9` 的 `homed` 位可能在成功写零后仍显示 `false`。v0.5.1 通过“本次连接收到成功 ACK，并读回 0 mm”建立会话级回零锁存。桥接重启后锁存会消失，因此下一次需要夹爪运动前，必须再次确认完全闭合并回零；这不是当前通信故障。

### 5. 验证 Intel RealSense D455

D455 已经完成：

- Windows PnP 识别；
- RealSense 工具识别；
- RGB/深度画面显示；
- 固件从 `5.15.1.55` 更新到 `5.17.3.10`。
- 固定到机械臂末端附近并用 USB 3.x 接入 Windows；
- 在隔离 Python 环境中同时采集 RGB、深度、加速度计和陀螺仪；
- 深度对齐到 RGB，并保存设备内参、深度到 RGB 外参、帧率和时间戳；
- 完成原始、高密度、视差偏移 50 和 100 四组近距对比；
- 验证每次测试结束后相机高级参数都会恢复原值。

已知设备信息：

```text
型号：Intel RealSense D455
序列号：260722303168
固件：5.17.3.10
USB：3.2
产品线：D400
```

当前实测 RGB 与深度约为 29.4–29.9 Hz，IMU 约为 198–202 Hz，RGB/深度帧时间差小于 0.6 ms。默认参数下 10 m 内有效深度像素约 24.3%；临时使用 disparity shift 100 时提升到约 40.8%，但有效范围被限制到约 0.18–0.41 m。因此保留“普通/中距”和“最后近抓取”两种配置，不把近距配置永久写入相机。

相机环境和诊断工具：

```text
Windows：C:\Desktop\PIPER\camera
服务器：/renyuanliu/MDE-research/PIPER/camera
Python SDK：pyrealsense2 2.58.1.10581
```

### 6. 建立服务器对 Windows 的持久远程控制

最终不再依赖用户手工保持 PowerShell 隧道窗口：

- Windows 本地账户：`piper_remote`
- Windows 主机名：`ENVY_Katana`
- 使用 SSH 密钥登录，不使用账户密码；
- Windows `sshd` 自动启动；
- `sshd` 只监听 `127.0.0.1:22`，不暴露到校园网；
- Windows 计划任务 `PIPER Remote Tunnel` 以 SYSTEM 身份开机启动；
- 反向隧道：学校服务器 `127.0.0.1:22022` → Windows `127.0.0.1:22`；
- 服务器 SSH 别名：`piper-windows`；
- 隧道断开后脚本每 5 秒自动重连。

Windows 当前实际运行的 SSH 服务：

```text
服务：sshd / Running / Auto
程序：C:\Program Files\OpenSSH\sshd.exe
版本：OpenSSH_for_Windows_10.0p2
监听：127.0.0.1:22
```

服务器配置位于：

```text
/root/.ssh/config
/root/.ssh/piper_windows_operator_ed25519
/root/.ssh/piper_windows_known_hosts
```

不要复制或展示私钥内容。

### 7. 建立开机自启的只读观察桥

Windows 计划任务 `PIPER Bridge Observe` 已配置为：

```text
身份：SYSTEM
状态：Running
模式：observe
端口：127.0.0.1:57845
固件档位：v189
夹爪：agx
```

它会在 Windows 启动后自动占用 CAN 适配器并读取状态，但没有机械臂或夹爪动作接口。控制模式不会自动启动。

两个计划任务今天结束时均为 `Running`；`LastTaskResult = 267009` 即十六进制 `0x41301`，含义是“任务正在运行”，不是失败。

### 8. 建立 D455 独立诊断环境

相机没有安装到机械臂控制用的 `.venv`，而是使用 `C:\Desktop\PIPER\camera\.venv`，避免相机依赖影响 CAN 控制。已固定版本：

```text
pyrealsense2 2.58.1.10581（与本机 RealSense Viewer SDK 一致）
numpy 2.5.1
Pillow 12.3.0
```

`realsense_diagnostic.py` 一次采集 RGB、对齐深度和 200 Hz IMU，保存 PNG 与 JSON。它支持临时设置 High Density 和 disparity shift，并在正常结束或异常退出时恢复测试前的完整高级设置。诊断期间没有进入控制模式，也没有发送任何机械臂或夹爪动作。

## 今天结束时的实机状态

以下为 2026-08-02 晚间的只读核验结果：

```text
桥版本：0.5.1
桥模式：observe
本地端口：57845
现场运动许可：false
活动命令：none

机械臂：connected / healthy
关节使能：[true, true, true, true, true, true]
状态：NORMAL
错误码：0
控制器状态：CAN_CTRL / MOVE_J
关节角（rad）：
[-0.029409, -0.009861, 0.013910, 0.604966, -0.021433, -0.008203]

夹爪：configured / feedback_present / healthy
开口：0.0098 m
驱动：disabled
homed：false（桥重启后会话锁存已清除，属预期）

D455：connected / USB 3.2 / firmware 5.17.3.10
RGB + depth：848×480 @ 30 Hz
IMU：accel + gyro @ 200 Hz
安装方式：末端附近机械固定，USB 直接接 Windows
相机参数：已恢复 Custom / emitter on / laser 150 / disparity shift 0
```

虽然控制器仍报告 `CAN_CTRL / MOVE_J` 且关节处于使能状态，但当前桥为 `observe`，没有发送动作的代码路径。

## 踩过的坑与解决办法

### 坑 1：Windows 安装依赖时无法访问 GitHub

症状：

```text
Failed to connect to github.com port 443 via 127.0.0.1
ModuleNotFoundError: No module named 'can'
```

原因：Windows 的 Git/代理指向不可用的 `127.0.0.1` 代理。

解决：把官方依赖的固定提交打成离线 ZIP，放进 `remote_control/vendor/`，由 `setup_windows.ps1` 离线安装。不要重新执行依赖 GitHub 在线下载的旧版 `requirements-windows.txt` 流程。

### 坑 2：桥接端口出现 `WinError 10013`

症状：CAN 已连接且状态已打印，但 HTTP 服务绑定端口失败。

解决：早期桥改为 `--port 0`，让 Windows 自动选择可用端口；开机观察任务现在使用已验证可用的固定端口 `57845`。不要再强行使用当时失败的端口。

### 坑 3：浏览器访问桥显示 `401`

浏览器请求 `/` 或 `/favicon.ico` 得到 `401` 是正常的令牌保护，不代表 CAN 失败。`/v1/health` 可用于健康检查；完整 `/v1/state` 必须带令牌，推荐始终使用 `piper_client.py`。

### 坑 4：更新包路径错误或只解压了一部分

症状：

```text
Expand-Archive: piper_remote_control_update_v0.2.0.zip 不存在
start_control.ps1 不存在
```

原因：ZIP 实际位置与命令中的路径不同，或只复制了部分 v0.2.0 文件。

解决：最终完整目录已经部署到 `C:\Desktop\PIPER\remote_control`，版本为 v0.5.1。明天不要重新下载整个文件夹；先检查 `bridge_core.py` 中的版本和 health 输出。

### 坑 5：`agx_cando` 打开设备时一直卡住

症状：

```text
OPENING
... agx_cando\bus.py line 205 in _open_device
```

原因：CAN 适配器被旧的 `piper_bridge.py` 进程占用，或 Windows USB 驱动句柄没有释放。

解决顺序：

1. 确保只运行一个 PIPER 桥；
2. 停止旧桥，等待 CAN 关闭；
3. 必要时重新插拔 USB-CAN；
4. 先运行最小 `can.Bus(...)` 打开/关闭测试；
5. 再启动桥。

不要同时运行观察桥和控制桥，它们会争用同一个 CAN 设备。

### 坑 6：控制桥看似卡在 `Connecting ...`

这次并不是机械臂没有响应，而是 `agx_cando` 正被旧进程或 USB 句柄阻塞。通过进程检查和最小 CAN 测试定位后，重新插拔适配器恢复。

### 坑 7：OpenSSH Server 在线安装非常慢并失败

经历过的问题：

- Windows Optional Feature 安装长时间停滞；
- 首版脚本在用户尚未创建时调用 `sshd -T -C user=...`，出现 `_ga_init, unable to resolve user piper_remote`；
- 内置 `C:\Windows\System32\OpenSSH\sshd.exe` 服务曾以错误 1067 退出；
- 从服务器 `scp` 时密码输入失败；
- 临时手工补丁没有匹配到目标代码。

最终解决：打包 Microsoft Win32-OpenSSH 离线版本，校验 SHA-256 后安装到 `C:\Program Files\OpenSSH`，再用修订后的引导脚本配置账户、ACL、loopback 监听和计划任务。当前实际服务为 `OpenSSH_for_Windows_10.0p2`，运行正常。

`C:\ProgramData\PiperRemote\install-result.json` 中可能仍记录旧的 System32 OpenSSH 9.5 路径；这是安装阶段遗留信息。以 `Get-CimInstance Win32_Service -Filter "Name='sshd'"` 的实时结果为准。

### 坑 8：旧隧道需要手工保持窗口

早期使用过服务器端口 8765/8766，并且每次桥端口变化都要手工更新隧道。现在已经改成 Windows 开机自启的 SSH 反向入口 22022，服务器可以直接 `ssh piper-windows`。旧的 8766 窗口不是当前恢复链路的一部分。

### 坑 9：夹爪成功回零但 `homed` 仍为 false

这是当前 PIPER `S-V1.8-9` 的反馈特性。v0.5.1 已兼容，但会话级回零锁存不会跨桥重启保存。每个新控制会话需要重新确认闭合和回零，不能仅根据旧状态直接发宽度动作。

### 坑 10：把末端两针口误认为相机接口

PIPER/原厂夹爪使用 24 V 与 CAN；D455 需要 USB 3.x 的供电和高速数据。相机现在机械安装在末端附近，但电气上仍由 USB 线直接连接 Windows，不能接两个两针圆口。

### 坑 11：计划任务显示 Running，但 22022 隧道不响应

一次较长相机采集期间，服务器上旧的隧道 `sshd` 子进程仍占有 `127.0.0.1:22022`，但已经不能转发数据；此时 Windows 计划任务仍可能显示 `Running`。这不是相机或 CAN 故障。

已增加服务器侧恢复工具。它先读取 SSH banner；只有 banner 超时且确认 22022 的监听者是 `sshd` 子进程时，才终止该陈旧会话，Windows 计划任务随后自动重连：

```bash
python3 /renyuanliu/MDE-research/PIPER/windows_remote_bootstrap/recover_server_tunnel.py
```

本次实测无需在 Windows 端操作，工具释放旧会话后约 1 秒恢复。若 30 秒仍未恢复，工具会明确提示重启 Windows 的 `PIPER Remote Tunnel` 任务。

## 如果明天自动恢复失败

先在 Windows 本机以管理员 PowerShell 执行：

```powershell
Get-Service sshd
Get-ScheduledTask -TaskName 'PIPER Remote Tunnel','PIPER Bridge Observe' |
    Format-Table TaskName,State
```

如果任务不是 `Running`：

```powershell
Start-Service sshd
Start-ScheduledTask -TaskName 'PIPER Remote Tunnel'
Start-ScheduledTask -TaskName 'PIPER Bridge Observe'
```

观察桥日志：

```powershell
Get-Content C:\ProgramData\PiperRemote\bridge-observe.log -Tail 100
Get-Content C:\ProgramData\PiperRemote\tunnel.log -Tail 100
```

如果 `ssh piper-windows` 不通，先在服务器运行：

```bash
python3 /renyuanliu/MDE-research/PIPER/windows_remote_bootstrap/recover_server_tunnel.py
```

仍不通再检查或重启 Windows 的 `PIPER Remote Tunnel`。如果 SSH 能通但 health 失败，检查 `PIPER Bridge Observe`、USB-CAN 和是否存在多个 `piper_bridge.py` 进程。

## 需要控制机械臂时

静态相机采集不需要进入控制模式。如果之后为手眼标定确实需要运动，必须先停止会占用 CAN 的观察任务：

```powershell
Stop-ScheduledTask -TaskName 'PIPER Bridge Observe'
cd C:\Desktop\PIPER\remote_control
powershell -ExecutionPolicy Bypass -File .\start_control.ps1 -Firmware v189 -Gripper agx
```

控制窗口中输入：

```text
ARM WORKSPACE CLEAR
```

结束控制桥后恢复只读观察任务：

```powershell
Start-ScheduledTask -TaskName 'PIPER Bridge Observe'
```

不要让观察桥与控制桥同时运行。`observe`、`control` 和现场许可的完整说明见 `PIPER/remote_control/README.md`。

## 明天的相机任务

### 已决定的布局

- D455：已固定在末端附近，作为腕部相机（eye-in-hand），USB 直接接 Windows。
- 第二台相机：后续作为固定场景相机（eye-to-hand），型号和安装位置尚未确认。

### 下一步顺序

1. 给 D455 USB 线增加应力释放，现场慢速检查机械臂完整工作范围，确保线缆不拉扯、不夹入关节。
2. 确认相机支架刚性和画面方向；之后锁紧支架，标定完成前不要再改位置。
3. 打印 ChArUco 标定板并准确测量方格尺寸。
4. 实现带现场逐姿态确认的手眼标定采集：每个静止姿态同步保存相机图像、标定板位姿和 PIPER 末端位姿。
5. 采集覆盖不同位置与姿态的 15–25 组样本，求解 `camera ↔ gripper` 外参并做留出姿态复投影验证。
6. 建立常驻但只读的 RGB-D/IMU + 机械臂 + 夹爪统一时间戳采集服务。
7. 根据任务距离动态选择深度档：普通/shift 50 用于接近，shift 100 只用于约 0.18–0.41 m 的最后近抓取。
8. 确认第二台相机型号，再设计固定场景相机的位置和 `camera ↔ base` 标定。

尚未完成：USB 线全工作区安全检查、眼在手上外参、统一时间戳采集服务、第二台相机型号与眼在手外标定。机械臂运动式标定必须等现场人员在旁、急停可用时再执行。

## 文件与版本位置

服务器：

```text
/renyuanliu/MDE-research/PIPER/remote_control
/renyuanliu/MDE-research/PIPER/camera
/renyuanliu/MDE-research/PIPER/windows_remote_bootstrap
/renyuanliu/MDE-research/PIPER/piper_remote_control_update_v0.2.0.zip
...
/renyuanliu/MDE-research/PIPER/piper_remote_control_update_v0.5.1.zip
/renyuanliu/MDE-research/PIPER/piper_windows_remote_bootstrap_v0.2.2.zip
```

Windows：

```text
C:\Desktop\PIPER\remote_control
C:\Desktop\PIPER\camera
C:\Desktop\PIPER\realsense
C:\ProgramData\PiperRemote
C:\Program Files\OpenSSH
```

当前 Windows 上三个核心控制文件与服务器副本的 SHA-256 已核对一致。Windows 与服务器的 `session-token.txt` 也已核对为同一内容，但令牌本身不写入本文。

本次提交只纳入代码、测试、说明文档和不含凭据的离线依赖。`session-token.txt`、SSH 私钥、部署专用公钥/主机指纹以及内含私钥的 `piper_windows_remote_bootstrap_v*.zip` 均由 `PIPER/.gitignore` 排除；它们仍保留在当前机器上，不影响已经部署的 Windows 服务。
